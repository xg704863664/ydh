spring:
  application:
    name: admin-server
  cloud:
    nacos:
      config:
        enabled: false
      discovery:
        server-addr: 47.94.92.201:15005
        metadata:
          management:
            context-path: ${server.servlet.context-path}/actuator
